# notas
```
Opciones:
      --help     Muestra ayuda       [booleano]      --version  Muestra número de versión
                                     [booleano]  -b, --base     Es la base de la tabla de
                 multiplicar
                           [número] [requerido]  -l, --listar   muestra la tabla de
                 multiplicar
                    [booleano] [defecto: false]  -h, --limite   Es el limite de la tabla de
                 multiplicar
                           [número] [requerido]
```
